IMAPAccount chalmers
# Address to connect to
Host localhost
Port 1143
user chalmers.se\\ashfaqf
PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.authinfo.gpg | awk '/machine localhost login ashfaqf/ {print $NF}'"
# To store the password in an encrypted file use PassCmd instead of Pass
# PassCmd "gpg2 -q --for-your-eyes-only --no-tty -d ~/.mailpass.gpg"
#
AuthMechs LOGIN
SSLType None

IMAPStore chalmers-remote
Account chalmers

MaildirStore chalmers-local
# The trailing "/" is important
Path ~/mail/
Inbox ~/mail/Inbox
SubFolders Legacy

Channel chalmers
Master :chalmers-remote:
Slave :chalmers-local:
Patterns *
# Automatically create missing mailboxes, both locally and on the server
Create Both
# Save the synchronization state files in the relevant directory
SyncState *
#Sync Pull
Expunge Both

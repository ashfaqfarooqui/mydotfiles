# Repository for my dot files

The packages can be restored using [[stow][https://www.gnu.org/software/stow/]].



Saving packages installed using pacman
'pacman -Qqen > packages-repository.txt'

'pacman -Qqem > packages-AUR.txt'

To install
'pacman -S - < pkglist.txt'


Require to change user name in zsh config.